# Deploy — resumo

Passo a passo completo em **COMECE-AQUI.md**. Em resumo:

1. **Push:** `cd ~/Desktop/mosaico-analitico && bash push.sh`
2. **Deploy:** importe o repo em **vercel.com/new** (detecta Next.js) → Deploy.
   Alternativa sem GitHub: `npx vercel --prod` na pasta.
